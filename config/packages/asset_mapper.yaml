framework:
    asset_mapper:
        public_prefix: '/undo-manager-jit-tail/dist/'
        # The paths to make available to the asset mapper.
        paths:
            - assets/
        missing_import_mode: strict

when@prod:
    framework:
        asset_mapper:
            missing_import_mode: warn
